import { MinuteHourPipe } from './minute-hour.pipe';

describe('MinuteHourPipe', () => {
  it('create an instance', () => {
    const pipe = new MinuteHourPipe();
    expect(pipe).toBeTruthy();
  });
});
